# Play Core

play.application.loader = utils.LwmApplicationLoader

play.crypto.secret = "WpP@1v@/ss/UDQ5XkG@depjlg?QcV4vzG@?1Pv[_:FqmnxFX:sGE`7C_>mn_BIl<"

play.application.langs = "en"

# Cors Filter

play.filters.cors {
  pathPrefixes = null
  allowedOrigins = null
  allowedHttpMethods = null
  allowedHttpHeaders = null
  supportsCredentials = true
  preflightMaxAge = 1 hour
}

# Session

play.http.session.httpOnly = false

play.http.session.domain = "localhost"

# Logging

akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"
}

# LWM Settings (Custom)

lwm.ldap.bindDN = "ou=people,ou=unix,dc=gm,dc=fh-koeln,dc=de"

lwm.ldap.groupDN = "ou=group,ou=unix,dc=gm,dc=fh-koeln,dc=de"

lwm.ldap.bindHost = "advldap.gm.fh-koeln.de"

lwm.ldap.username = ""

lwm.ldap.password = ""

lwm.ldap.bindPort = 636

lwm.ldap.cron = ""

lwm.sessions.lifetime = 30

lwm.sessions.lifetime = ${?LWM_SESSION_TIMEOUT}

lwm.controllers.timeout = 45

lwm.controllers.timeout = ${?LWM_CONTROLLER_TIMEOUT}

lwm.namespace = "http://localhost:9000"

lwm.namespace = ${?LWM_NAMESPACE}

lwm.store.path = ""

lwm.store.path = ${?LWM_STORE_PATH}

lwm.store.backup = ""

lwm.store.backup = ${?LWM_STORE_BACKUP}

lwm.store.cron = ""

lwm.store.cron = ${?LWM_STORE_CRON}

# Database

database {
  dataSourceClass = org.postgresql.ds.PGSimpleDataSource
  properties = {
    url = ""
    databaseName = ""
    user = ""
    password = ""
  }
  numThreads = 10
  keepAliveConnection = true // ensures that the database does not get dropped while we are using it
}

database_test {
  dataSourceClass = org.postgresql.ds.PGSimpleDataSource
  properties = {
    url = "jdbc:postgresql://localhost:5432/lwm"
    databaseName = "lwm"
    user = "postgres"
    password = ""
  }
  numThreads = 10
  keepAliveConnection = true // ensures that the database does not get dropped while we are using it
}
