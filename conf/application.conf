# Play Core

play {
  i18n.langs = ["en"]
  http.secret.key = "WpP@1v@/ss/UDQ5XkG@depjlg?QcV4vzG@?1Pv[_:FqmnxFX:sGE`7C_>mn_BIl<"

  session {
    secure = true
    httpOnly = true
    domain = "localhost"
  }

  filters.cors {
    allowedOrigins = ["localhost", "gm.fh-koeln.de"]
    allowedHttpMethods = ["POST", "GET", "DELETE", "PUT"]
    allowedHttpHeaders = ["Authorization"]
    preflightMaxAge = 1 hour
  }

  filters.disabled += play.filters.csrf.CSRFFilter
  filters.enabled += play.filters.cors.CORSFilter
}

# LWM Settings (Custom)

lwm {
  backup {
    path = ""
    cron = ""
  }

  schedule {
    populations = 20
    generations = 100
    elites = 10
  }

  blacklist {
    year = "" // can either be current or next. see `NaturalDescribableYear.apply` for more details
    cron = ""
  }

  semester {
    year = "" // can either be current or next. see `NaturalDescribableYear.apply` for more details
    cron = ""
  }
}

# Database

database {
  dataSourceClass = org.postgresql.ds.PGSimpleDataSource
  properties = {
    url = ""
    databaseName = ""
    user = ""
    password = ""
  }
  numThreads = 10
  keepAliveConnection = true // ensures that the database does not get dropped while we are using it
}