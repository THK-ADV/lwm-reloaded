# Play Core

play.http.secret.key = "WpP@1v@/ss/UDQ5XkG@depjlg?QcV4vzG@?1Pv[_:FqmnxFX:sGE`7C_>mn_BIl<"

## Internationalisation
# https://www.playframework.com/documentation/latest/JavaI18N
# https://www.playframework.com/documentation/latest/ScalaI18N
# ~~~~~
# Play comes with its own i18n settings, which allow the user's preferred language
# to map through to internal messages, or allow the language to be stored in a cookie.
play.i18n {
  # The application languages
  langs = [ "en" ]

  # Whether the language cookie should be secure or not
  #langCookieSecure = true

  # Whether the HTTP only attribute of the cookie should be set to true
  #langCookieHttpOnly = true
}

# Cors Filter

//play.filters.cors {
//  pathPrefixes = null
//  allowedOrigins = null
//  allowedHttpMethods = null
//  allowedHttpHeaders = null
//  supportsCredentials = true
//  preflightMaxAge = 1 hour
//}

# Session

//play.http.session {
//  httpOnly = false
//  domain = "localhost"
//}

# Logging

#akka {
#  loggers = ["akka.event.slf4j.Slf4jLogger"]
#  loglevel = "INFO"
#}

# LWM Settings (Custom)

//lwm.sessions.lifetime = 30
//
//lwm.controllers.timeout = 45
//
//lwm.backup {
//  path = ""
//  cron = ""
//}
//
//lwm.schedule {
//  populations = 20
//  generations = 100
//  elites = 10
//}
//
//lwm.blacklist {
//  year = "" // can either be current or next. see `NaturalDescribableYear.apply` for more details
//  cron = ""
//}
//
//lwm.semester {
//  year = "" // can either be current or next. see `NaturalDescribableYear.apply` for more details
//  cron = ""
//}

# Database

database {
  dataSourceClass = org.postgresql.ds.PGSimpleDataSource
  properties = {
    url = "jdbc:postgresql://139.6.56.147:5432/lwm"
    databaseName = "lwm"
    user = "lwm"
    password = "abcde123"
  }
  numThreads = 10
  keepAliveConnection = true // ensures that the database does not get dropped while we are using it
}

//database_test {
//  dataSourceClass = org.postgresql.ds.PGSimpleDataSource
//  properties = {
//    url = "jdbc:postgresql://localhost:5432/lwm"
//    databaseName = "lwm"
//    user = "postgres"
//    password = ""
//  }
//  numThreads = 10
//  keepAliveConnection = true // ensures that the database does not get dropped while we are using it
//}